{
  "name": "Strip",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string"
    },
    "template_id": {
      "type": "string"
    },
    "photo_urls": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "rendered_strip_url": {
      "type": "string"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "expires_at": {
      "type": "string",
      "format": "date-time"
    },
    "saved": {
      "type": "boolean",
      "default": true
    },
    "filter_applied": {
      "type": "string",
      "enum": [
        "none",
        "sepia",
        "bw",
        "vintage",
        "vivid",
        "cool",
        "warm"
      ],
      "default": "none"
    },
    "is_live": {
      "type": "boolean",
      "default": false
    },
    "video_urls": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "rendered_video_url": {
      "type": "string"
    }
  },
  "required": [
    "user_id",
    "template_id",
    "photo_urls",
    "created_at",
    "saved"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}