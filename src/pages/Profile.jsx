import React from "react";
import { Crown, LogOut } from "lucide-react";
import { base44 } from "@/api/base44Client";
import { useAuth } from "@/lib/AuthContext";
export default function Profile(){const{user}=useAuth();const plan=user?.plan||"free";return <div className="max-w-xl"><h1 className="font-heading text-3xl font-extrabold">Profile</h1><section className="mt-7 rounded-[18px] border border-[#D8D9DC] bg-white p-5"><p className="text-xs font-bold uppercase tracking-wider text-[#8B8D93]">Account</p><p className="mt-4 font-bold">{user?.full_name||"Your account"}</p><p className="text-sm text-[#8B8D93]">{user?.email}</p></section><section className="mt-4 rounded-[18px] bg-[#15161A] p-6 text-white"><Crown size={22} className="text-[#B7C7DA]"/><p className="mt-6 text-sm text-[#D8D9DC]">Current plan</p><h2 className="font-heading text-2xl font-extrabold capitalize">{plan}</h2>{plan==="free"?<><p className="mt-2 text-sm text-[#D8D9DC]">Unlimited sessions, every template, one full year of saved strips.</p><p className="mt-5 text-xs text-[#B7C7DA]">Premium checkout will be available with PayMongo.</p></>:<p className="mt-2 text-sm text-[#D8D9DC]">Your next renewal is {user?.plan_expires_at?new Date(user.plan_expires_at).toLocaleDateString():"coming up"}.</p>}</section><button onClick={()=>base44.auth.logout("/login")} className="mt-7 flex items-center gap-2 text-sm font-bold text-[#55575E]"><LogOut size={17}/>Log out</button></div>}